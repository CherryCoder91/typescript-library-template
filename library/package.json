{
  "name": "library",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "rimraf dist/",
    "build": "npm run clean && tsc",
    "build:watch": "npm run clean && tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "rimraf": "^6.0.1"
  }
}